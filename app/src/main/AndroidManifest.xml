<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.amjadaboabdou.teacher">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:name=".BaseApplication"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.Login" />
        <activity
            android:name=".Activities.Teacher_Signup"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".Activities.Payment_fees"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".Activities.Home"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".Activities.Search"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".Activities.Teacher_rating" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".Activities.splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.ContactActivity" />
        <activity android:name=".Activities.PrivacyActivity"></activity>
    </application>

</manifest>